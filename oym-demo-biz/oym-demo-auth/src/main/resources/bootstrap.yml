spring:
  profiles:
    active: dev
  #应用配置
  application:
    #名称: OAuth2认证授权服务
    name: oym-demo-auth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        # 指定group
        group: DEFAULT_GROUP
        # 文件后缀，默认为properties
        file-extension: yml