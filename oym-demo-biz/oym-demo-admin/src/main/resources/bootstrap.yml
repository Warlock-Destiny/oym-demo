spring:
  profiles:
    active: dev
  application:
    name: zyd-demo-admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        # 指定group
        group: DEFAULT_GROUP
        # 文件后缀，默认为properties
        file-extension: yml